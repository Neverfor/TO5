<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE struts PUBLIC
    "-//Apache Software Foundation//DTD Struts Configuration 2.0//EN"
    "http://struts.apache.org/dtds/struts-2.0.dtd">

<struts>

	<constant name="struts.devMode" value="true" />
	<constant name="struts.custom.i18n.resources" value="global" />

	<package name="default" extends="struts-default">
		<!-- Tiles -->
		<result-types>
			<result-type name="tiles"
				class="org.apache.struts2.views.tiles.TilesResult" />
		</result-types>

		<!-- Index -->
		<action name="index">
			<result type="tiles">welcome</result>
		</action>
	</package>

	<!-- package frame voor actions achter login-muur -->
	<package name="accountAware" extends="struts-default">
		<!-- interceptor regelt authenticatie controle -->
		<interceptors>
			<interceptor name="securityInterceptor" class="utils.SecurityInterceptor" />
			<interceptor-stack name="securityStack">
				<interceptor-ref name="securityInterceptor" />
				<interceptor-ref name="defaultStack" />
			</interceptor-stack>
		</interceptors>

		<!-- Global results zijn globale error en/of fallback pagina's -->
		<global-results>
			<result name="login" type="redirectAction">
				<param name="actionName">gebruikerspanel</param>
				<param name="namespace">/gebruiker</param>
			</result>
			<result name="error" type="tiles">error</result>
		</global-results>
	</package>

	<!-- Includes -->
	<include file="accountActions/account-struts.xml" />
	<include file="veilingActions/veiling-struts.xml" />

</struts>
